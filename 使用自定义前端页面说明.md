 # 认知模型测试服务启动说明
 ## 启动 open-webui 后端，用于提供后端服务
 ```bash
docker run -d -p 3000:8080 -p 8080:8080 \
    -e OPENAI_API_BASE_URL=http://host.docker.internal:8642/v1 \
    -e OPENAI_API_KEY=change-me-local-dev \
    -e ENABLE_OLLAMA_API=false \
    --add-host=host.docker.internal:host-gateway \
    -v open-webui:/app/backend/data \
    --name open-webui --restart always \
    openwebui/open-webui:0.9.6
```
### note
-e OPENAI_API_BASE_URL=http://host.docker.internal:8642/v1 配置hermes接口  
-e OPENAI_API_KEY=change-me-local-dev 配置 hermes key，默认为change-me-local-dev  
-e ENABLE_OLLAMA_API=false 关闭本地大模型API服务  
-p 8080:8080 为认知模型测试页面提供后端服务端口  
--add-host=host.docker.internal:host-gateway 将 host.docker.internal 解析为宿主机 IP，使容器内可访问宿主机上的服务  
## 启动 open-webui 认知模型测试页面
```bash
docker run -d -p 3001:80 \
    -e BACKEND_URL=http://host.docker.internal:8080 \
    --add-host=host.docker.internal:host-gateway \
    --name owui-frontend \
    open-webui-frontend:local
```
### note
open-webui-frontend:local 为认知模型测试页面镜像  
项目地址：https://github.com/catfanta/open-webui/tree/feature/chat-background-opacity  

# 认知模型测试前端镜像构建说明
```bash
cd open-webui
npm run build
docker build -f Dockerfile.frontend -t open-webui-frontend:local .
```